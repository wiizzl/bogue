{% extends 'base.html.twig' %}

{% block title %}Étudiants{% endblock %}

{% block body %}
    <section>
        <h2>Liste des étudiants</h2>
        <p><a href="{{ path('app_student_new') }}">Ajouter un étudiant</a></p>
        <form method="get" action="{{ path('app_student_index') }}">
            <label for="showArchived">
                <input id="showArchived" name="showArchived" type="checkbox" value="1" {% if showArchived %}checked{% endif %}>
                Afficher les archivés
            </label>
            <button type="submit">Filtrer</button>
        </form>
    </section>

    <section class="table-section">
        <table>
            <thead>
                <tr>
                    <th>Prenom</th>
                    <th>Nom</th>
                    <th>Promotion</th>
                    <th>Option</th>
                    <th class="table-actions-header">Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for student in students %}
                <tr>
                    <td>{{ student.firstName }}</td>
                    <td>{{ student.lastName }}</td>
                    <td>{{ student.promotion.year }}</td>
                    <td>{{ student.major.code }}</td>
                    <td class="table-actions">
                        <a href="{{ path('app_student_edit', {'id': student.id}) }}" aria-label="Modifier l'étudiant" title="Modifier">
                            {{ ux_icon('pen-line', { 'class': 'action-icon' }) }}
                            <span class="visually-hidden">Modifier</span>
                        </a>
                        {{ include('student/_delete_form.html.twig', {'student': student}) }}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">Aucun étudiant enregistré.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </section>
{% endblock %}
