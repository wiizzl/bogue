{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <section>
        <p><a href="{{ path('app_user_new') }}">Ajouter un utilisateur</a></p>
    </section>

    <section class="table">
        <table>
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Email</th>
                    <th>Rôles</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.fullName }}</td>
                    <td>{{ user.email }}</td>
                    <td>
                        {{ user.role and user.role.label ? user.role.label : 'Utilisateur' }}
                    </td>
                    <td>
                        <a href="{{ path('app_user_show', {'id': user.id}) }}" class="button outline small" aria-label="Voir l'utilisateur" title="Voir">
                            {{ ux_icon('eye') }}
                        </a>
                        <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="button outline small" aria-label="Modifier l'utilisateur" title="Modifier">
                            {{ ux_icon('pen-line') }}
                        </a>
                        {% if app.user and app.user.id != user.id %}
                            {{ include('user/_delete_form.html.twig', {'user': user}) }}
                        {% endif %}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">Aucun utilisateur enregistré.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </section>
{% endblock %}
